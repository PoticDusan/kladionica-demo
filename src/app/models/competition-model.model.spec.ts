import { CompetitionModel } from './competition-model.model';

describe('CompetitionModel', () => {
  it('should create an instance', () => {
    expect(new CompetitionModel()).toBeTruthy();
  });
});
